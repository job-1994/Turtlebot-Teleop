#include "teleop/teleop_listener.h"

teleop_listener::teleop_listener()
  {
//    sub = n.subscribe("chatter", 1000, &MyListener::chatterCallback,this);
  }
teleop_listener::~teleop_listener(){}



int main(int argc, char **argv)
  { 
    ros::init(argc, argv, "listener");
    ros::NodeHandle n;
//    MyListener *listen = new MyListener(n);
    ros::spin();

  return 0;
}
